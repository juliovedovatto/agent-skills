# youtube-transcript

Fetch transcripts from YouTube videos for summarization and analysis.

## Setup

This skill depends on `youtube-transcript-plus`. Install dependencies before first use:

```bash
cd ~/.pi/agent/skills/youtube-transcript
npm install
```

Without `npm install`, the skill cannot fetch transcripts.

## Usage

See `SKILL.md` for full usage details.
